
  @cherrypy.expose
  def tst(self,  strMatrix):
    self.classificador.myclassify(strMatrix)



  @cherrypy.expose
  def plotImage(self):
    np.savetxt(self.DIR + 'arrayImageBinary.txt', self.array, delimiter=' ', newline='\n', fmt='%i')